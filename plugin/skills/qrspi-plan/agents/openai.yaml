interface:
  display_name: "QRSPI Plan"
  short_description: "Turn the structure into an implementation plan"
  default_prompt: "Use $qrspi-plan to produce the tactical implementation plan."

policy:
  allow_implicit_invocation: false
